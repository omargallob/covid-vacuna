module.exports = (phase, { defaultConfig }) => {
  return {
    i18n: {
      // These are all the locales you want to support in
      // your application
      locales: ['es'],
      defaultLocale: 'es',
    }
  }
}